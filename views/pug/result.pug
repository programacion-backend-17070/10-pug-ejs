doctype
html
  head
    link(
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/normalize.css@8.0.1/normalize.css"
      type="text/css"
    )
    link(
      rel="stylesheet"
      href="/static/css/style.css"
      type="text/css")
  body
    nav
      ul
        a(href='/pug')
          li Inicio
        a(href='/pug/add')
          li Agregar pelicula
    div(class="container")
      h1(class="emoji-header") 😏
      p La pelicula #{movie} ha sido agregada con exito!